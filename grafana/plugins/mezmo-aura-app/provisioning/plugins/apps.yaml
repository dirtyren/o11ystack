apiVersion: 1

apps:
  - type: 'mezmo-mezmoaura-app'
    org_id: 1
    org_name: 'mezmo'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key
